<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F7"
    android:fitsSystemWindows="true"
    tools:context=".EmployeetActivity">

    <!-- 1. Thanh AppBar ở trên cùng --><com.google.android.material.appbar.AppBarLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:theme="@style/Theme.AppCompat.DayNight.DarkActionBar"
    android:background="#F5F5F7"
    app:elevation="0dp">

    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbar_employee"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        app:title="Employee Management"
        app:titleTextColor="#333333"
        app:navigationIcon="?attr/homeAsUpIndicator"
        app:navigationIconTint="#333333" />

</com.google.android.material.appbar.AppBarLayout>

    <!-- 2. Nội dung chính --><LinearLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    android:paddingStart="20dp"
    android:paddingEnd="20dp">

    <!-- 3. Thanh tìm kiếm (Search Bar) --><androidx.cardview.widget.CardView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="16dp"
    android:layout_marginBottom="16dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="@android:color/white">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingVertical="10dp">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@android:drawable/ic_menu_search"
            app:tint="#8A8A8A" />

        <EditText
            android:id="@+id/et_search_employee"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:hint="Search employee..."
            android:textColor="#333333"
            android:textColorHint="#B0B0B0"
            android:textSize="16sp"
            android:background="@android:color/transparent"
            android:singleLine="true"
            android:imeOptions="actionSearch"/>

    </LinearLayout>
</androidx.cardview.widget.CardView>

    <!-- 4. Tiêu đề "Employee List" --><TextView
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:fontFamily="sans-serif-medium"
    android:text="Employee List"
    android:textColor="#333333"
    android:textSize="18sp"
    android:textStyle="bold"
    android:layout_marginBottom="12dp"/>

    <!-- 5. Danh sách nhân viên (RecyclerView) --><!-- Đây là nơi bạn sẽ thêm RecyclerView. Tôi sẽ giữ nó đơn giản với một LinearLayout ví dụ. --><androidx.recyclerview.widget.RecyclerView
    android:id="@+id/rv_employee_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:clipToPadding="false"
    android:paddingBottom="16dp"
    app:layoutManager="androidx.recyclerview.widget.LinearLayoutManager"
    tools:listitem="@layout/item_employee" /> <!-- Đây là item layout cho mỗi nhân viên --></LinearLayout>

    <!-- 6. Nút FAB để thêm nhân viên mới --><com.google.android.material.floatingactionbutton.FloatingActionButton
    android:id="@+id/fab_add_employee"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom|end"
    android:layout_margin="24dp"
    app:backgroundTint="#6A5AED"
    app:tint="@android:color/white"
    android:src="@android:drawable/ic_input_add"
    app:maxImageSize="30dp"
    app:elevation="6dp"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>